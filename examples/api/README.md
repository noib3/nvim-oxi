# api

```lua
-- The `Greetings` command and the insert mode mapping from `hi` to `hello`
-- will be set in the call to `require`.

local api = require("api")

api.open_window()
api.close_window()
```
