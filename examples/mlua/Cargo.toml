[package]
name = "lua"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
nvim-oxi = { path = "../../nvim-oxi", features = ["mlua"] }
mlua = { version = "0.8", features = ["luajit", "vendored"] }
