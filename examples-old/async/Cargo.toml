[package]
name = "async"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
nvim-oxi = { path = "../../nvim-oxi", features = ["loop"] }
tokio = { version = "1.0", features = ["full"] }
