[package]
name = "tests"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[features]
nightly = ["nvim-oxi/nightly"]

[dependencies]
all_asserts = "2.3"
nvim-oxi = { path = "../crates/nvim-oxi", features = ["test"] }
